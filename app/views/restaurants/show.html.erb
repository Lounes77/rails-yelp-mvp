<h1><%= @restaurant.name %></h1>
<ul>
  <li><%= @restaurant.address %></li>
  <li><%= @restaurant.phone_number %></li>
</ul>

<h2>Reviews</h2>
<p> <%= link_to "Add review", new_restaurant_review_path(@restaurant) %></p>
<ul>
  <% @reviews.each do |review| %>
    <li> <%= review.rating %></li> - <li><%= review.content %></li> - <%= link_to "Delete 🚮", review_path(review), data: { turbo_method: :delete, turbo_confirm: 'Are you sure do you want to delete this review?' } %>
  <% end %>
  <%= link_to "Back to list", restaurants_path %>
</ul>
